/**
 * Extends the base Chat Message class to implement additional system-specific logic.
 * @param {object} data - The initial data object provided to the document creation request.
 * @param {object} options - Additional options which modify the creation request.
 * @param {documents.BaseUser} user - The User requesting the document creation.
 * @augments ChatMessage
 */
export default class TitanChatMessage extends ChatMessage {

}