/**
 * Returns whether the current user is on a Chromium browser.
 * @returns {boolean} Whether the current user is on a Chromium browser.
 */
export default function isChromium() {
   return window.chrome;
}