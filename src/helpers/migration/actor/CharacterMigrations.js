/**
 * Ordered list of migrations shared by all Character Actor documents (Player and NPC).
 * Each entry upgrades the document's schema from the previous version to the specified version.
 * @type {Array<{version: number, migrate: function(foundry.abstract.Document, object): object}>}
 */
export default [];
