# Remove Dead Dependencies Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove every unused dependency from `package.json`, re-sync the lockfile, and correct the skill note — with the build and both linters still passing.

**Architecture:** Delete the 6 confirmed-dead devDependencies; empirically test removal of the 2 borderline Stylelint syntaxes (`postcss-html`, `postcss-scss`) by running Stylelint and restoring only if it breaks; keep `@types/node` (it types the Node globals in the `.mjs` config files for the editor). Re-sync `package-lock.json` via `npm install`. Verify with `npm run build`/`eslint`/`stylelint`.

**Tech Stack:** npm, Vite 5, ESLint 9 (flat config), Stylelint 16. **No test framework** — verification is the three project gates + `grep`.

**Reference:** Spec at `docs/superpowers/specs/2026-05-30-remove-dead-dependencies-design.md`.

> **Commit policy:** The user controls commits. Each task ends with a commit step — run it only if the user approved committing; otherwise leave changes in the working tree.

---

## File map

- `package.json` — remove dead devDependencies (Task 1).
- `package-lock.json` — regenerated by `npm install` (Task 1).
- `.claude/skills/titan-codebase/SKILL.md` — correct the `foundry-vtt-types` note (Task 2).

No `src/`, `jsconfig.json`, `eslint.config.js`, or `.stylelintrc.json` changes.

---

## Task 1: Remove dead dependencies and re-sync the lockfile

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json` (via `npm install`)

- [ ] **Step 1: Replace the `devDependencies` block**

In `package.json`, find this block:

```json
   "devDependencies": {
      "@league-of-foundry-developers/foundry-vtt-types": "^13.346.0-beta.20250812191140",
      "@rollup/plugin-node-resolve": "^16.0.3",
      "@sveltejs/vite-plugin-svelte": "^4.0.0",
      "@types/node": "^22.10.02",
      "@typhonjs-config/eslint-config": "^0.6.0",
      "@typhonjs-fvtt/eslint-config-foundry.js": "^0.8.0",
      "autoprefixer": "^10.4.19",
      "esdoc": "^1.1.0",
      "esdoc-ecmascript-proposal-plugin": "^1.0.0",
      "esdoc-standard-plugin": "^1.0.0",
      "eslint": "^9.3.0",
      "eslint-plugin-jsdoc": "^48.2.5",
      "eslint-plugin-svelte": "^2.39.0",
      "postcss-html": "^1.7.0",
      "postcss-scss": "^4.0.9",
      "sass-embedded": "^1.100.0",
      "stylelint": "^16.12.0",
      "stylelint-config-html": "^1.1.0",
      "stylelint-config-standard-scss": "^16.0.0",
      "svelte": "^5.0.0",
      "svelte-preprocess": "^6.0.2",
      "vite": "^5.4.10"
   },
```

Replace it with (removes the 6 confirmed-dead packages AND the 2 borderline `postcss-*` packages; keeps `@types/node`):

```json
   "devDependencies": {
      "@rollup/plugin-node-resolve": "^16.0.3",
      "@sveltejs/vite-plugin-svelte": "^4.0.0",
      "@types/node": "^22.10.02",
      "autoprefixer": "^10.4.19",
      "eslint": "^9.3.0",
      "eslint-plugin-jsdoc": "^48.2.5",
      "eslint-plugin-svelte": "^2.39.0",
      "sass-embedded": "^1.100.0",
      "stylelint": "^16.12.0",
      "stylelint-config-html": "^1.1.0",
      "stylelint-config-standard-scss": "^16.0.0",
      "svelte": "^5.0.0",
      "svelte-preprocess": "^6.0.2",
      "vite": "^5.4.10"
   },
```

- [ ] **Step 2: Re-sync the lockfile**

Run: `npm install`
Expected: completes successfully; `package-lock.json` is updated (removed packages + orphaned transitives drop out). If `npm install` reports an error, stop and report it.

- [ ] **Step 3: Run Stylelint — the decisive gate for `postcss-html`/`postcss-scss`**

Run: `npm run stylelint`
Expected: clean (no output / exit 0), meaning the `stylelint-config-html`/`stylelint-config-standard-scss` shareable configs provide the `.svelte`/`.scss` custom syntaxes transitively and the direct `postcss-*` entries were redundant.

**If instead Stylelint errors** with a message about not resolving a custom syntax (e.g. "Cannot resolve custom syntax module postcss-html" or "...postcss-scss"), the package was needed directly. Restore the offending one(s): re-add the matching line(s) into the `devDependencies` block in correct alphabetical position (between `eslint-plugin-svelte` and `sass-embedded`):

```json
      "postcss-html": "^1.7.0",
      "postcss-scss": "^4.0.9",
```

Then run `npm install` again and re-run `npm run stylelint` until it is clean. Note in your report which `postcss-*` package(s), if any, had to be restored.

- [ ] **Step 4: Run the build gate**

Run: `npm run build`
Expected: Vite build succeeds (emits `index.js`/`style.css` at repo root, "✓ built" line). Proves no build dependency was removed.

- [ ] **Step 5: Run the ESLint gate**

Run: `npm run eslint`
Expected: `0 errors` (the pre-existing JSDoc-style warnings are unchanged; the run completing with 0 errors proves the removed `@typhonjs-*` eslint configs were unused).

- [ ] **Step 6: Verify the confirmed-dead names are gone from `package.json`**

Run: `grep -nE "foundry-vtt-types|typhonjs-config|eslint-config-foundry|esdoc" package.json`
Expected: no output.

- [ ] **Step 7: Verify the confirmed-dead names are gone from `package-lock.json`**

Run: `grep -nE "foundry-vtt-types|@typhonjs-config/eslint-config|eslint-config-foundry\.js|\"esdoc\"|esdoc-ecmascript-proposal-plugin|esdoc-standard-plugin" package-lock.json`
Expected: no output. (If any hit appears, inspect it — a real dependency entry means a kept package still depends on it transitively, which is fine to leave; report any such case. There should be none.)

- [ ] **Step 8: Commit** (only if approved)

```bash
git add package.json package-lock.json
git commit -m "chore(deps): remove unused devDependencies (foundry-vtt-types, typhonjs eslint configs, esdoc)"
```

(If a `postcss-*` package was restored in Step 3, adjust the commit message to note it was kept.)

---

## Task 2: Correct the `foundry-vtt-types` note in the skill

**Files:**
- Modify: `.claude/skills/titan-codebase/SKILL.md`

- [ ] **Step 1: Replace the stale mismatch note**

In `.claude/skills/titan-codebase/SKILL.md`, find:

```markdown
- Foundry **v14** (`system.json` compatibility: minimum 13 / verified 14 / maximum 14). Note: the
  `@league-of-foundry-developers/foundry-vtt-types` dev dependency is still **v13** — a known
  type-package/runtime version mismatch, not a code issue.
```

Replace with:

```markdown
- Foundry **v14** (`system.json` compatibility: minimum 13 / verified 14 / maximum 14). Editor types
  come from the live Foundry install — `jsconfig.json` includes `../../../../foundry/common/**` and
  `foundry/public/scripts/**`; there is no `foundry-vtt-types` dependency.
```

- [ ] **Step 2: Verify no stale `foundry-vtt-types` mismatch wording remains in the skill**

Run: `grep -rn "foundry-vtt-types" .claude/skills/titan-codebase/`
Expected: a single hit — the new bullet's "no `foundry-vtt-types` dependency" wording. No "mismatch" / "still **v13**" text remains.

- [ ] **Step 3: Commit** (only if approved)

```bash
git add .claude/skills/titan-codebase/SKILL.md
git commit -m "docs(skill): correct foundry-vtt-types note (dependency removed; types from live install)"
```

---

## Self-review notes (author)

- **Spec coverage:** confirmed-dead removal (Task 1 Step 1) ✓; borderline `postcss-*` empirical test with restore path (Task 1 Step 3) ✓; `@types/node` kept (documented rationale — editor types for `.mjs` config files) ✓; lockfile re-sync (Step 2) ✓; build/eslint/stylelint gates (Steps 3–5) ✓; package.json + lockfile grep verification (Steps 6–7) ✓; skill note correction (Task 2) ✓. Spec "out of scope" items (version bumps, dep reclassification, a11y work) untouched.
- **Placeholder scan:** No TBD/TODO. The conditional restore path in Step 3 gives exact lines and placement, not a vague "handle if needed."
- **Consistency:** The removed names in the grep gates (Steps 6–7) exactly match the lines deleted in Step 1. The kept-package list in the replacement block matches the spec's "confirmed used" + `@types/node`.
